<!--components/header/header.wxml-->
   <view class="header">
        <l-search-bar placeholder="搜索" show-cancel="{{false}}" clear="{{true}}"/>
        <image class="l-like" src="{{g_like}}" />
      </view>
